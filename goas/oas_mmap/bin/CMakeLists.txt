cmake_minimum_required(VERSION 2.8)

SET(BINARY_NAME temp)

LINK_DIRECTORIES(${PROJECT_SOURCE_DIR}/../lib/)

SET(SOURCES
    temp.c
)

ADD_EXECUTABLE(${BINARY_NAME} ${SOURCES})

INCLUDE_DIRECTORIES(../include/)

TARGET_LINK_LIBRARIES(${BINARY_NAME} oas)




